% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/struct.R
\name{build_zcor}
\alias{build_zcor}
\title{Build a \code{zcor} Matrix for Extended Working Correlation Structures}
\usage{
build_zcor(
  corstr,
  id,
  waves,
  maxwave = NULL,
  bandwidth = NULL,
  m = NULL,
  subgroup = NULL,
  block = NULL,
  individual = NULL
)
}
\arguments{
\item{corstr}{character string naming the working correlation
structure. One of \code{"independence"}, \code{"exchangeable"}, \code{"ar1"},
\code{"unstructured"}, "\code{toeplitz}", "\code{banded-toeplitz}", "\code{banded-unstructured}", "\code{m-dependent}", "\code{nested-exchangeable}", "\code{pairwise-grouped-exchangeable}" or "\code{block-exchangeable}". See \strong{Details} for the
argument(s) required by each.}

\item{id}{cluster identifier (the GEE unit, e.g., the cluster/site in a
cluster randomized trial)}

\item{waves}{actual time/position of each observation (not the row order)
within each cluster. For \code{"block-exchangeable"}, can recur within the same
cluster, since several individuals share the same period (wave).}

\item{maxwave}{total number of possible waves. Deduced from \code{max(waves)}
when \code{NULL}. Used by every structure except \code{"block_exchangeable"}.}

\item{bandwidth}{truncation lag \code{k}: correlation is fixed at 0 beyond this
lag. Required when \code{corstr} is \code{"banded-toeplitz"} or
\code{"banded-unstructured"}; ignored otherwise.}

\item{m}{integer; shared lag cutoff for the common m-dependent parameter.
Required when \code{corstr = "m-dependent"}; ignored otherwise.}

\item{subgroup}{vector of length \code{maxwave} giving the subgroup of each
wave. Required when \code{corstr = "nested_exchangeable"}; ignored otherwise.

\if{html}{\out{<div class="sourceCode">}}\preformatted{e.g., c("L","L","R","R") for 2 left-eye measurements, 2 right-eye
measurements for 4 waves; or the visit number if there are multiple
measurements per visit.
}\if{html}{\out{</div>}}}

\item{block}{vector of length \code{maxwave} giving the group of each wave.
Required when \code{corstr = "pairwise_grouped_exchangeable"}; ignored
otherwise.

\if{html}{\out{<div class="sourceCode">}}\preformatted{e.g.,: c(1,1,2,2,2,3) for 3 blocks and 6 waves.
}\if{html}{\out{</div>}}}

\item{individual}{identifier of the individual within each cluster,
persisting across periods for individuals followed in a cohort design.
Required when \code{corstr = "block_exchangeable"}; ignored otherwise.}
}
\value{
A numeric matrix suitable for the \code{zcor} argument of
\code{geeglm(..., corstr = "userdefined")}: one row per observed pair within a
cluster, one column per correlation parameter to estimate.
}
\description{
\code{build_zcor()} builds the \code{zcor} matrix based on \code{corstr}, ready to be passed
to \code{\link[geepack:geeglm]{geepack::geeglm()}} as
\code{geeglm(..., corstr = "userdefined", zcor = build_zcor(...))}.
}
\details{
\code{corstr} determines both the dispatch target and which of the remaining
arguments are required:\tabular{ll}{
   \code{corstr} \tab requires \cr
   \code{"toeplitz"} \tab \code{id}, \code{waves} \cr
   \code{"banded-toeplitz"} \tab \code{id}, \code{waves}, \code{bandwidth} \cr
   \code{"banded-unstructured"} \tab \code{id}, \code{waves}, \code{bandwidth} \cr
   \code{"m-dependent"} \tab \code{id}, \code{waves}, \code{m} \cr
   \code{"nested-exchangeable"} \tab \code{id}, \code{waves}, \code{subgroup} \cr
   \code{"pairwise-grouped-exchangeable"} \tab \code{id}, \code{waves}, \code{block} \cr
   \code{"block-exchangeable"} \tab \code{id}, \code{waves}, \code{individual} \cr
}


\code{data} must be sorted by \code{id} (contiguous per cluster) and, within each
cluster, in the same row order used to build \code{waves} (and \code{individual},
when applicable) — see the package vignette for details on required
sorting per structure.
}
\seealso{
\code{\link[geepack:geeglm]{geepack::geeglm()}}, \code{\link[geepack:genZcor]{geepack::genZcor()}}
}
